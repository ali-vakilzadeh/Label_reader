node_modules/
dist/
.env
*.log

# Runtime data — SQLite databases and generated artefacts
data/*.db
data/*.db-shm
data/*.db-wal
data/legalArmenianMap.json

# Operator-uploaded photos and rendered catalog shots
uploads/
public/catalog/*
!public/catalog/.gitkeep

# reference_data/ IS committed — it ships with the server.
# Its .backups/ is not: the middleware writes a timestamped copy there before
# every supervisor-approved edit (UI_messaging_protocol.md §9.2), which is
# runtime state, not source.
reference_data/.backups/
